specVersion: "v1"
serviceId: "project-dayum"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project-dayum-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
